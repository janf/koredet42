<h1>Listing items</h1>

<table>
  <thead>
    <tr>
      <th>ID</th> 
      <th>ItemType</th>
      <th>ItemTypeName</th>
      <th>Itemname</th>
      <th>Isunique</th>
      <th>Appxvalue</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.id %></td> 
        <td><%= item.itemtype %></td>
        <td><%= item.item_types_id.item_type_name %></td>
        <td><%= item.itemname %></td>
        <td><%= item.isunique %></td>
        <td><%= item.appxvalue %></td>
        <td><%= link_to 'Show', item %></td>
        <td><%= link_to 'Edit', edit_item_path(item) %></td>
        <td><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Item', new_item_path %>
