<%= form_for(@item_type) do |f| %>
  <% if @item_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_type.errors.count, "error") %> prohibited this item_type from being saved:</h2>

      <ul>
      <% @item_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :itemclass %><br>
    <%= f.text_field :itemclass %>
  </div>
  <div class="field">
    <%= f.label :itemtype %><br>
    <%= f.text_field :itemtype %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
